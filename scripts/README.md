# scripts — 辅助脚本

> 项目开发与维护相关的工具脚本集合。

## 脚本列表

| 脚本 | 说明 | 使用方式 |
|------|------|---------|
| `scaffold-dotnet.sh` | 从 `templates/dotnet-service/` 模板创建 .NET 后端子项目 | `bash scripts/scaffold-dotnet.sh <ProjectName>` |

## 规范

- 脚本使用 Bash（可在 Git Bash / WSL 环境下运行）
- 所有脚本应支持 `-h` 或 `--help` 查看使用说明
