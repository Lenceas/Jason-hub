<header class="header">
  <div class="container header__inner">
    <a href="/" class="header__logo" aria-label="回到首页">
      <img src="/images/logo.svg" alt="Jason Logo" width="100" height="30" class="header__logo-img" />
    </a>
    <p class="header__subtitle">个人主页</p>
  </div>
</header>

<style>
  .header {
    padding: 1rem 0;
    border-bottom: 1px solid var(--color-border);
    background: var(--color-bg);
  }

  .header__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .header__logo {
    display: flex;
    align-items: center;
    transition: opacity var(--transition);
  }

  .header__logo:hover {
    opacity: 0.75;
  }

  .header__logo-img {
    width: 80px;
    height: auto;
  }

  .header__subtitle {
    font-size: 0.8rem;
    color: var(--color-text-muted);
    font-weight: 450;
  }

  @media (min-width: 640px) {
    .header {
      padding: 1.25rem 0;
    }

    .header__logo-img {
      width: 100px;
    }

    .header__subtitle {
      font-size: 0.875rem;
    }
  }
</style>
