<div id="user-auth" class="user-auth">
  <div id="user-auth-loading" class="user-auth__loading"></div>
  <a id="user-auth-login" href="https://api-auth.lujiesheng.cn/login?redirect=https://lujiesheng.cn" class="user-auth__login" style="display:none">
    登录
  </a>
  <div id="user-auth-panel" class="user-auth__panel" style="display:none">
    <button class="user-auth__avatar-btn" id="user-auth-toggle" aria-label="用户菜单">
      <span class="user-auth__avatar" id="user-auth-avatar"></span>
      <span class="user-auth__name" id="user-auth-name"></span>
      <span class="user-auth__role" id="user-auth-role-badge"></span>
      <svg class="user-auth__arrow" width="12" height="12" viewBox="0 0 12 12" fill="none">
        <path d="M3 5L6 8L9 5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </button>
    <div class="user-auth__dropdown" id="user-auth-dropdown">
      <a href="https://api-auth.lujiesheng.cn/logout?redirect=https://lujiesheng.cn" class="user-auth__dropdown-item" id="user-auth-logout">
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none">
          <path d="M5 13H2.5C2.102 13 1.904 13 1.755 12.922C1.622 12.853 1.517 12.748 1.448 12.615C1.37 12.466 1.37 12.268 1.37 11.87V2.13C1.37 1.732 1.37 1.534 1.448 1.385C1.517 1.252 1.622 1.147 1.755 1.078C1.904 1 2.102 1 2.5 1H5M9.5 10L13 7M13 7L9.5 4M13 7H5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        退出登录
      </a>
    </div>
  </div>
</div>

<script is:inline>
(function() {
  var AUTH_API = 'https://api-auth.lujiesheng.cn';
  var LOGIN_URL = AUTH_API + '/login?redirect=' + encodeURIComponent(window.location.origin);

  var loadingEl = document.getElementById('user-auth-loading');
  var loginEl = document.getElementById('user-auth-login');
  var panelEl = document.getElementById('user-auth-panel');
  var nameEl = document.getElementById('user-auth-name');
  var avatarEl = document.getElementById('user-auth-avatar');
  var roleBadgeEl = document.getElementById('user-auth-role-badge');
  var toggleBtn = document.getElementById('user-auth-toggle');
  var dropdownEl = document.getElementById('user-auth-dropdown');
  var logoutEl = document.getElementById('user-auth-logout');

  var hoverTimer = null;
  var isOpen = false;

  loginEl.href = LOGIN_URL;
  logoutEl.href = AUTH_API + '/logout?redirect=' + encodeURIComponent(window.location.origin);

  function openDropdown() {
    clearTimeout(hoverTimer);
    dropdownEl.classList.add('user-auth__dropdown--open');
    isOpen = true;
  }

  function closeDropdown() {
    dropdownEl.classList.remove('user-auth__dropdown--open');
    isOpen = false;
  }

  function closeAfterDelay() {
    hoverTimer = setTimeout(closeDropdown, 200);
  }

  // 点击头像切换
  toggleBtn.addEventListener('click', function(e) {
    e.stopPropagation();
    if (isOpen) closeDropdown(); else openDropdown();
  });

  // 悬停展开
  panelEl.addEventListener('mouseenter', openDropdown);
  panelEl.addEventListener('mouseleave', closeAfterDelay);
  dropdownEl.addEventListener('mouseenter', function() { clearTimeout(hoverTimer); });
  dropdownEl.addEventListener('mouseleave', closeAfterDelay);

  // 点击外部关闭
  document.addEventListener('click', function(e) {
    if (!panelEl.contains(e.target)) closeDropdown();
  });

  // 检查登录状态
  fetch(AUTH_API + '/api/v1/auth/me', { credentials: 'include' })
    .then(function(res) {
      if (res.ok) return res.json();
      throw new Error('not logged in');
    })
    .then(function(data) {
      loadingEl.style.display = 'none';
      panelEl.style.display = '';

      var displayName = data.username || '?';
      nameEl.textContent = displayName;
      avatarEl.textContent = displayName.charAt(0).toUpperCase();
      roleBadgeEl.textContent = data.role === 'admin' ? '管理员' : '用户';
    })
    .catch(function() {
      loadingEl.style.display = 'none';
      loginEl.style.display = '';
    });
})();
</script>

<style>
  .user-auth {
    position: relative;
    display: flex;
    align-items: center;
    min-width: 60px;
    justify-content: flex-end;
  }

  .user-auth__loading {
    width: 20px;
    height: 20px;
    border: 2px solid var(--color-border, #e5e7eb);
    border-top-color: var(--color-primary, #3b82f6);
    border-radius: 50%;
    animation: user-auth-spin 0.6s linear infinite;
  }

  @keyframes user-auth-spin {
    to { transform: rotate(360deg); }
  }

  .user-auth__login {
    font-size: 0.875rem;
    color: var(--color-primary, #3b82f6);
    text-decoration: none;
    font-weight: 500;
    padding: 0.4rem 1rem;
    border: 1px solid var(--color-primary, #3b82f6);
    border-radius: 6px;
    transition: all 0.2s;
  }

  .user-auth__login:hover {
    background: var(--color-primary, #3b82f6);
    color: #fff;
  }

  .user-auth__panel {
    position: relative;
  }

  .user-auth__avatar-btn {
    display: flex;
    align-items: center;
    gap: 0.35rem;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0.25rem 0.5rem;
    border-radius: 6px;
    transition: background 0.15s;
    white-space: nowrap;
  }

  .user-auth__avatar-btn:hover {
    background: var(--color-border-hover, #f3f4f6);
  }

  .user-auth__avatar {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    background: var(--color-primary, #3b82f6);
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.75rem;
    font-weight: 600;
    flex-shrink: 0;
  }

  .user-auth__name {
    font-size: 0.85rem;
    color: var(--color-text, #1f2937);
    font-weight: 500;
    max-width: 80px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .user-auth__role {
    font-size: 0.7rem;
    color: var(--color-text-muted, #9ca3af);
    background: var(--color-bg-muted, #f3f4f6);
    padding: 0.1rem 0.45rem;
    border-radius: 4px;
    font-weight: 450;
    line-height: 1.4;
  }

  .user-auth__arrow {
    transition: transform 0.2s;
    color: var(--color-text-muted, #9ca3af);
  }

  .user-auth__dropdown--open .user-auth__arrow {
    transform: rotate(180deg);
  }

  .user-auth__dropdown {
    position: absolute;
    top: calc(100% + 6px);
    right: 0;
    background: var(--color-bg, #fff);
    border: 1px solid var(--color-border, #e5e7eb);
    border-radius: 8px;
    box-shadow: 0 4px 16px rgba(0,0,0,0.08);
    min-width: 140px;
    opacity: 0;
    visibility: hidden;
    transform: translateY(-4px);
    transition: all 0.15s ease;
    z-index: 100;
  }

  .user-auth__dropdown--open {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  .user-auth__dropdown-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.6rem 0.75rem;
    font-size: 0.8rem;
    color: var(--color-text-muted, #6b7280);
    text-decoration: none;
    transition: background 0.15s, color 0.15s;
    border-radius: 8px;
  }

  .user-auth__dropdown-item:hover {
    background: var(--color-bg-hover, #f9fafb);
    color: var(--color-text, #1f2937);
  }
</style>
